services:
  db:
    image: mysql:8.0
    container_name: faceapp-db
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: faceapp
    ports:
      - "3307:3306"
    volumes:
      - db_data:/var/lib/mysql
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql
      
  web:
    build: .
    container_name: flask-faceapp
    ports:
      - "5000:5000"
    volumes:
      - .:/app
    depends_on:
      - db
    restart: unless-stopped

volumes:
  db_data: